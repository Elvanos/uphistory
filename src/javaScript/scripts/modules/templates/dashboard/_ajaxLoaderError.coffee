_ajaxLoaderError = (app, module, subModule) ->
    
    globals = app.globals
    
    console.log 'error meesage'
    

    # TODO Finish the error function
    # Disable scroll detection after spitting out error message
    #globals.scrollDetectionLock = false
    
export default _ajaxLoaderError